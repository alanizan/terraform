# MacOS files
    .DS_Store

    # Terraform files
    .terraform/
    *.tfstate
    *.tfstate.backup
    .terraform.lock.hcl
    # Ignore the top-level aws notes folder
    /aws/

    # Exclude sensitive credential files
    *_accessKeys.csv
